<?xml version="1.0" encoding="utf-8" ?>
<local:BaseView xmlns="http://xamarin.com/schemas/2014/forms"
                xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                xmlns:local="clr-namespace:ExrinSampleMobileApp.View.Base"
								Title="Main"
                x:Class="ExrinSampleMobileApp.View.MainView">
  <StackLayout Padding="20">
    <Label Text="Hello, this the main page" HorizontalTextAlignment="Center" />
		<ListView ItemsSource="{Binding VisualState.ObservableList}" />
    <Button Text="About Page" Command="{Binding AboutCommand}" />
  </StackLayout>
</local:BaseView>